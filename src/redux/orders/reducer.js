import { DUMMY_ORDERS } from "../../constants/dummyData";
import {} from "./actionTypes";

export default orderReducer = (state = DUMMY_ORDERS, action) => {
	switch(action.type) {

		default: return state;
	}
}