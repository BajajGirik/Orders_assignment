import {} from "./actionTypes";

const initialState = {
	isLoggedIn: false,
	data: {},
};

export default authReducer = (state = initialState, action) => {
	switch(action.type) {

		default: return state;
	}
}